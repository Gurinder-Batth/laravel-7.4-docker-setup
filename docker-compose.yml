version: '3.8'
services:
  laravelserver:
   build:
    context: ./php
   image: "laravel-image"
   ports: 
    - 1234:80
   volumes:
    - ./src:/var/www/html
    - ./apache/default.conf:/etc/apache2/sites-enabled/000-default.conf
    - ./phpMyAdmin/bash.sh:/var/www/bash.sh
    - ./phpMyAdmin/src:/var/www/phpMyAdmin

  laraveldb:
   image: mysql
   restart: unless-stopped
   ports: 
    - "4306:3306"
   environment:
    MYSQL_ROOT_PASSWORD: "password"
    MYSQL_DATABASE: "laravel"
    MYSQL_USER: "user"
    MYSQL_PASSWORD: "password"
   volumes:
    - ./dblaravelvol:/var/lib/mysql
  
  


    

